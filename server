
from http import server
import socket    

if __name__ == "__main__":
    ip = "192.168.0.1w"
    port= 1234

    server  = socket.socket(socket.AF_INET , socket.SOCK_STREAM)
    server.connect((ip,port))

    string = imput("Enter string: ")
    server.send(bytes(string, "utf-8"))
